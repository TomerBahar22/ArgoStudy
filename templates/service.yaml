apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  ports:
  - port: {{ .Values.ports.port }}
    targetPort: {{ .Values.ports.targetPort }}
    type: {{ .Values.ports.type }}
  selector:
    app: guestbook-ui
